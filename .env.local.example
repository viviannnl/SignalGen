# MongoDB Atlas connection string. Keep the real value private.
MONGODB_URI=

# Google Cloud project created for SignalGen.
GOOGLE_CLOUD_PROJECT=signalgen-496700

# Later: used by the agent to create branches/commits/PRs against LetterGen.
GITHUB_TOKEN=
TARGET_REPO=viviannnl/ai-cover-letter

# Later: optional if we need to query Vercel deployment metadata directly.
VERCEL_TOKEN=
